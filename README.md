# UAGO-3C (Universal Adaptive Geometric Observer - Three-Cycle Core)

Проект для анализа и визуализации фрактальных изображений с использованием символьной регрессии.

## 📋 Обзор

UAGO-3C анализирует геометрические инварианты изображений и генерирует математические формулы, описывающие их структуру. Проект использует как локальные методы символьной регрессии, так и API Mistral для генерации формул.

## 🚀 Возможности

- Анализ изображений и извлечение геометрических инвариантов
- Генерация математических формул с помощью:
  - Локальной символьной регрессии (PySR)
  - API Mistral (опционально)
- Интерактивная визуализация результатов с использованием JSXGraph
- Трехэтапный процесс анализа:
  1. **Открытие**: Анализ изображения и извлечение инвариантов
  2. **Воплощение**: Генерация визуализации
  3. **Валидация**: Анализ и уточнение результатов

## 🔧 Использование Mistral API

Mistral API используется как опциональный метод генерации формул. Для его использования:

1. Получите API ключ на [Mistral AI](https://mistral.ai/)
2. Укажите ключ в конфигурации или переменных окружения
3. Включите использование API в настройках

### Пример конфигурации:

```json
{
  "use_mistral_api": true,
  "mistral_api_key": "ваш_api_ключ",
  "mistral_model": "mistral-large-latest"
}
```

## ⚙️ Установка

1. Клонируйте репозиторий:
   ```bash
   git clone https://github.com/yourusername/uago-3c.git
   cd uago-3c/project
   ```

2. Установите зависимости:
   ```bash
   pip install -r requirements.txt
   ```

3. (Опционально) Установите Julia и необходимые пакеты для PySR:
   ```julia
   using Pkg
   Pkg.add("PyCall")
   Pkg.add("SymbolicRegression")
   ```

## 🏃‍♂️ Запуск

```bash
python main.py путь/к/изображению.png
```

## 🛠️ Зависимости

- Python 3.8+
- PySR
- NumPy
- SciPy
- scikit-learn
- Pillow
- requests
- Jinja2
- jsxgraph (для визуализации)

## 📁 Структура проекта

```
uago-3c/
├── project/
│   ├── src/
│   │   ├── uago_core.py         # Основной движок
│   │   ├── symbolic_regressor.py # Генерация формул
│   │   ├── invariant_measurer.py # Анализ инвариантов
│   │   └── jsx_visualizer.py    # Визуализация
│   ├── config/
│   │   └── uago_config.json     # Конфигурация
│   ├── data/
│   │   └── samples/             # Примеры изображений
│   ├── output/
│   │   ├── visualizations/      # Сохраненные визуализации
│   │   └── reports/             # Отчеты анализа
│   └── main.py                  # Точка входа
└── README.md
```

## 🤝 Вклад

Приветствуются пул-реквесты и сообщения об ошибках.

## 📄 Лицензия

MIT
```

### Где именно используется Mistral API:

1. В файле [symbolic_regressor.py](cci:7://file:///c:/Users/mungo/CascadeProjects/uago-3c/project/symbolic_regressor.py:0:0-0:0) в функции [_generate_with_mistral()](cci:1://file:///c:/Users/mungo/CascadeProjects/uago-3c/project/src/symbolic_regressor.py:40:0-101:18):
   - Формируется промпт с геометрическими инвариантами
   - Отправляется запрос к API Mistral
   - Обрабатывается ответ и извлекается формула

2. В основном цикле обработки (в [uago_core.py](cci:7://file:///c:/Users/mungo/CascadeProjects/uago-3c/project/uago_core.py:0:0-0:0)) при включенной опции `use_mistral_api`:
   - Если API ключ указан и доступен, используется Mistral для генерации формул
   - В случае ошибки или отсутствия ключа автоматически переключается на локальную регрессию

3. В конфигурационном файле можно управлять:
   - Включением/отключением API
   - Выбором модели Mistral
   - Настройками таймаутов и других параметров запроса